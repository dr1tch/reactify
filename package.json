{
  "name": "react-super",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "version-packages": "changeset version",
    "release": "yarn --cwd packages/ui build && yarn changeset publish"
  }
}
